<van-tabbar safe-area-inset-bottom="{{isIPhoneX}}" wx:if="{{ !hide }}" active="{{ active }}" bind:change="onChange" z-index="-1" fixed="false">
  <van-tabbar-item wx:for="{{ list }}" wx:key="index" icon="{{ item.icon }}">{{
    item.text
  }}</van-tabbar-item>
</van-tabbar>
